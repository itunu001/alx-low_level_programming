#include <stdio.h>

/**
 * main - entry point
 *
 * Description: Prints a string of text on standard output
 *
 * Return: 0 on success, non-zero otherwise.
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
